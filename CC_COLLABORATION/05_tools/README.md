# 05_TOOL_DEFINITIONS.md
# 工具定义规范

> 版本：v1.0
> 最后更新：2024-12-09
> 状态：Draft - 待审核
> 说明：本文档定义所有 Slash Commands、Skills、Subagents 的规格，审核通过后再实际实现

---

## 1. 概述

本文档定义三类工具的规格：
- **Slash Commands**：用户直接调用的命令
- **Skills**：Claude Code 自动触发的能力
- **Subagents**：独立执行复杂任务的子代理

### 1.1 工具状态说明

| 状态 | 说明 |
|------|------|
| 📝 Draft | 规格草稿，待审核 |
| ✅ Approved | 审核通过，可实现 |
| 🚧 Implementing | 正在实现 |
| ✔️ Done | 已完成并可用 |

---

## 2. Slash Commands

### 2.1 /new-feature

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P0 |
| 阶段 | Phase 1 Kickoff |

**用途**：初始化新功能模块目录和基础文档

**使用方式**：
```bash
/new-feature {feature-name}
# 示例
/new-feature user-subscription
```

**执行逻辑**：
```
1. 验证 feature-name 格式（kebab-case）
2. 检查目录是否已存在
3. 创建目录结构：
   Docs/{feature-name}/
   ├── 00_CONTEXT.md
   ├── 30_PROGRESS_LOG.yaml
   └── 31_DAILY_SUMMARY/
4. 生成初始文档内容
5. 输出创建结果
6. 提示下一步操作
```

**输入参数**：
| 参数 | 必填 | 说明 |
|------|------|------|
| feature-name | 是 | 功能代号，kebab-case 格式 |

**输出**：
```
✅ 功能模块 "{feature-name}" 已创建

📁 创建的文件：
  - Docs/{feature-name}/00_CONTEXT.md
  - Docs/{feature-name}/30_PROGRESS_LOG.yaml
  - Docs/{feature-name}/31_DAILY_SUMMARY/

📋 下一步：
  1. 完善 00_CONTEXT.md
  2. 进入 Phase 2 Spec
```

---

### 2.2 /iresume

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P0 |
| 阶段 | Phase 5 Code |

**用途**：从 checkpoint 恢复上下文，继续工作

**使用方式**：
```bash
/iresume {feature-name}
# 示例
/iresume user-subscription
```

**执行逻辑**：
```
1. 读取 Docs/{feature-name}/30_PROGRESS_LOG.yaml
2. 解析 cc_checkpoint 信息
3. 读取 context_files 列表中的文件
4. 输出当前状态和 next_step
5. 准备继续工作
```

**输出**：
```
🔄 恢复上下文：user-subscription

📍 当前状态：
  - Phase: 5 (Code)
  - 进度: 60% (12/20 tasks)
  - 最后操作: {last_action}

📋 下一步：
  {next_step}

📄 已加载上下文文件：
  - 00_CONTEXT.md
  - 10_DESIGN_FINAL.md
  - 20_DEV_PLAN.md
```

---

### 2.3 /daily-summary

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P0 |
| 阶段 | Phase 5 Code |

**用途**：从 PROGRESS_LOG 生成今日工作总结

**使用方式**：
```bash
/daily-summary
/daily-summary {feature-name}
```

**执行逻辑**：
```
1. 读取 PROGRESS_LOG.yaml
2. 筛选今日完成的任务
3. 计算进度统计
4. 生成 DAILY_SUMMARY 文件
5. 保存到 31_DAILY_SUMMARY/{date}.md
```

**输出文件格式**：
```markdown
# Daily Summary - {YYYY-MM-DD}

## 进度概览
| 类别 | 数量 |
|------|------|
| 总任务数 | X |
| 已完成 | Y |
| 完成率 | Z% |

## 今日完成
- [TASK-001] 任务描述
- [TASK-002] 任务描述

## 明日计划
- [TASK-003] 待完成任务

## 阻塞项
无
```

---

### 2.4 /check-progress

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P1 |
| 阶段 | Phase 5 Code |

**用途**：查看当前功能模块的进度状态

**使用方式**：
```bash
/check-progress {feature-name}
```

**输出**：
```
📊 进度报告：user-subscription

Phase 进度：
  ✅ Phase 0: Foundation (100%)
  ✅ Phase 1: Kickoff (100%)
  ✅ Phase 2: Spec (100%)
  🔄 Phase 3: Demo (60%)
  ⏳ Phase 4: Design (0%)
  ⏳ Phase 5: Code (0%)
  ⏳ Phase 6: Test (0%)
  ⏳ Phase 7: Deploy (0%)

总体进度: 35% (14/40 tasks)

当前阻塞: 无
```

---

### 2.5 /gen-demo

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P2 |
| 阶段 | Phase 3 Demo |

**用途**：根据 UI SPEC 生成可交互 Demo

**使用方式**：
```bash
/gen-demo {feature-name}
```

**执行逻辑**：
```
1. 读取 11_UI_FLOW_SPEC.md
2. 读取 _ui_system 规范
3. 生成 Vue 组件到 playgrounds/{feature-name}/
4. 生成 Mock API 数据
5. 启动预览
```

---

### 2.6 /run-tests

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P2 |
| 阶段 | Phase 6 Test |

**用途**：执行测试计划并生成报告

**使用方式**：
```bash
/run-tests {feature-name}
```

**执行逻辑**：
```
1. 读取 40_TEST_PLAN.md
2. 执行测试用例（使用 Chrome MCP 或 API 测试）
3. 收集测试结果
4. 生成 41_TEST_REPORT.md
```

---

### 2.7 /release

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P2 |
| 阶段 | Phase 7 Deploy |

**用途**：生成发布说明并打 Git Tag

**使用方式**：
```bash
/release {feature-name} {version}
# 示例
/release user-subscription v1.0.0
```

**执行逻辑**：
```
1. 读取 PROGRESS_LOG 和 TEST_REPORT
2. 生成 50_RELEASE_NOTE.md
3. 创建 Git Tag
4. 输出发布摘要
```

---

### 2.8 /init-project

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P3 |
| 阶段 | Phase 0 Foundation |

**用途**：初始化项目级 _system 目录

**使用方式**：
```bash
/init-project
```

**执行逻辑**：
```
1. 创建 Docs/_system/ 目录结构
2. 生成 PROJECT_PROFILE.yaml（模板）
3. 生成 PROJECT_CONTEXT.md（模板）
4. 创建子目录
```

---

### 2.9 /sync-progress 🆕

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P0 |
| 阶段 | Phase 5 Code |
| 所属 | PM 工具包 |

**用途**：从各功能模块的 PROGRESS_LOG.yaml 同步数据到前端 projectRegistry.ts

**使用方式**：
```bash
/sync-progress
# 同步所有功能模块

/sync-progress {feature-name}
# 同步指定功能模块
```

**执行逻辑**：
```
1. 扫描 Docs/ 目录下所有功能模块
2. 读取每个模块的 30_PROGRESS_LOG.yaml
3. 解析 meta、phase_X、cc_checkpoint、stats 数据
4. 转换为 GanttFeatureEntry 格式
5. 更新 vue-app/src/data/projectRegistry.ts 中的 ganttFeatures[]
6. 输出同步结果
```

**数据映射**：
```
PROGRESS_LOG.yaml                →  projectRegistry.ts
─────────────────────────────────────────────────────────
meta.feature                     →  id
meta.feature_name                →  name
meta.owner                       →  owner
meta.started_at                  →  startDate
最后 phase 的 end_date           →  endDate
phase_X_xxx.tasks                →  schedule[]
stats.summary                    →  progressSummary
文件路径                         →  progressLogPath
```

**输出**：
```
🔄 同步进度数据...

📊 扫描到 3 个功能模块：
  ✅ project-dashboard-system (62%)
  ✅ gantt-dashboard (100%)
  ✅ slash-commands (0%)

📁 已更新文件：
  - vue-app/src/data/projectRegistry.ts

✅ 同步完成！甘特图看板数据已更新。
```

---

### 2.10 /daily-standup 🆕

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P0 |
| 阶段 | Phase 5 Code |
| 所属 | PM 工具包 |

**用途**：聚合各功能模块的 PROGRESS_LOG，生成项目级每日站会摘要

**使用方式**：
```bash
/daily-standup
# 生成今日站会摘要

/daily-standup {date}
# 生成指定日期的站会摘要
# 示例: /daily-standup 2025-12-09
```

**执行逻辑**：
```
1. 扫描 Docs/ 目录下所有功能模块
2. 读取每个模块的 30_PROGRESS_LOG.yaml
3. 筛选今日（或指定日期）completed_at 的任务
4. 识别 blocked 状态的任务
5. 提取明日待办（wip 状态任务）
6. 汇总里程碑信息
7. 生成 PROJECT_DAILY_STANDUP.yaml
8. 更新 projectRegistry.ts 中的 currentDailyStandup
```

**输出文件**：`Docs/_system/PROJECT_DAILY_STANDUP.yaml`
```yaml
meta:
  date: 2025-12-09
  generated_by: "CC"
  generated_at: "2025-12-09T20:00:00+08:00"

highlights:
  - feature_id: "project-dashboard-system"
    feature_name: "AI 协作开发框架"
    summary: "完成执行清单视图重构（DASH-006）"

blockers:
  - feature_id: "payment-system"
    issue: "等待第三方支付 SDK 审批"
    blocked_since: "2025-12-08"

tomorrow:
  - feature_id: "slash-commands"
    plan: "开始设计 /iresume 命令"

milestones:
  - date: "2025-12-10"
    content: "甘特图看板上线"
```

**同步到前端**：
```typescript
// 自动更新 projectRegistry.ts
export const currentDailyStandup: DailyStandup = {
  date: '2025-12-09',
  highlights: [...],
  blockers: [...],
  tomorrow: [...],
  milestones: [...]
}
```

**输出**：
```
📋 生成每日站会摘要...

📅 日期: 2025-12-09

✨ 今日进展 (2):
  • [AI 协作开发框架] 完成执行清单视图重构（DASH-006）
  • [甘特图看板] 完成瀑布式甘特图视图开发

⚠️ 阻塞问题 (0):
  无

📌 明日计划 (2):
  • [slash-commands] 开始设计 /iresume 命令
  • [project-dashboard-system] 完善测试用例

📁 已更新文件：
  - Docs/_system/PROJECT_DAILY_STANDUP.yaml
  - vue-app/src/data/projectRegistry.ts

✅ 站会摘要已生成！
```

---

## 3. Skills

### 3.1 ui_demo

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P1 |
| 触发 | Phase 3 Demo |

**用途**：根据 UI SPEC + UI System 生成可交互 Demo

**触发条件**：
- 用户请求生成 Demo
- 执行 /gen-demo 命令

**输入**：
- `11_UI_FLOW_SPEC.md`
- `_ui_system/` 规范文件

**输出**：
- `playgrounds/{feature-name}/` 目录
- 可运行的 Vue 组件

**执行流程**：
```
1. 解析 UI SPEC 中的页面和组件定义
2. 根据 UI System 选择合适的组件
3. 生成 Vue 组件代码
4. 生成 Mock 数据
5. 配置路由和入口
```

---

### 3.2 review_alignment

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P1 |
| 触发 | 每个 task 完成后 |

**用途**：检查代码与设计文档的一致性

**触发条件**：
- 完成一个开发任务后
- 手动请求检查

**检查项**：
- [ ] API 接口与 DESIGN 一致
- [ ] 数据结构与 DESIGN 一致
- [ ] UI 组件与 SPEC 一致
- [ ] 命名与规范一致

**输出**：
```
✅ 一致性检查通过

或

⚠️ 发现不一致：
  - API /users 响应格式与 DESIGN 不符
  - 建议：将 { user: {...} } 改为 { data: {...} }
```

---

### 3.3 progress_updater

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P0 |
| 触发 | 任务状态变更时 |

**用途**：自动更新 PROGRESS_LOG 状态

**触发条件**：
- 开始新任务
- 完成任务
- 遇到阻塞

**执行动作**：
```
1. 更新任务状态（pending → wip → done）
2. 添加时间戳
3. 更新统计信息
4. 更新 cc_checkpoint
```

---

### 3.4 doc_generator

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P1 |
| 触发 | 需要生成标准文档时 |

**用途**：根据模板生成任意标准文档

**支持的文档类型**：
- CONTEXT
- DESIGN
- UI_FLOW_SPEC
- API_SPEC
- DEV_PLAN
- TEST_PLAN
- RELEASE_NOTE

---

### 3.5 spec_validator

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P2 |
| 触发 | Phase 2 Spec 完成时 |

**用途**：检查 SPEC 文档的完整性和一致性

**检查项**：
- [ ] 必填字段是否完整
- [ ] 数据类型定义是否正确
- [ ] 边界条件是否覆盖
- [ ] 示例数据是否有效

---

### 3.6 mock_api_generator

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P2 |
| 触发 | Phase 3 Demo |

**用途**：根据 API SPEC 生成 Mock API

**输出**：
- Mock API 服务配置
- 示例数据文件
- API 路由定义

---

### 3.7 test_runner

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P2 |
| 触发 | Phase 6 Test |

**用途**：执行测试计划中的测试用例

**支持的测试类型**：
- UI 测试（Chrome MCP）
- API 测试
- 单元测试

---

### 3.8 changelog_updater

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P2 |
| 触发 | 文档变更时 |

**用途**：自动更新文档的 CHANGELOG

---

## 4. Subagents

### 4.1 spec_writer

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P1 |
| 阶段 | Phase 2 Spec |

**用途**：根据 CONTEXT 自动生成 UI_FLOW_SPEC 或 API_SPEC

**输入**：
- `00_CONTEXT.md`
- `_ui_system/` 规范

**输出**：
- `11_UI_FLOW_SPEC.md` 和/或 `11_API_SPEC.md`

**工作流程**：
```
1. 读取 CONTEXT 文档
2. 分析功能范围和用户场景
3. 根据 has_ui/has_api 决定输出
4. 生成规格文档草稿
5. 等待人工审核
```

---

### 4.2 progress_tracker

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P1 |
| 阶段 | Phase 5 Code |

**用途**：解析 PROGRESS_LOG，生成 DAILY_SUMMARY

**输入**：
- `30_PROGRESS_LOG.yaml`

**输出**：
- `31_DAILY_SUMMARY/{date}.md`

---

### 4.3 test_plan_writer

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P2 |
| 阶段 | Phase 6 Test |

**用途**：根据 SPEC 生成测试计划

**输入**：
- `11_UI_FLOW_SPEC.md`
- `11_API_SPEC.md`

**输出**：
- `40_TEST_PLAN.md`

---

### 4.4 release_summarizer

| 属性 | 值 |
|------|-----|
| 状态 | 📝 Draft |
| 优先级 | P2 |
| 阶段 | Phase 7 Deploy |

**用途**：汇总进度和测试报告，生成发布说明

**输入**：
- `30_PROGRESS_LOG.yaml`
- `41_TEST_REPORT.md`

**输出**：
- `50_RELEASE_NOTE.md`

---

## 5. 实现优先级

### P0 - 必须实现

| 工具 | 类型 | 阶段 | 说明 |
|------|------|------|------|
| /new-feature | Command | Phase 1 | 初始化功能模块 |
| /iresume | Command | Phase 5 | 恢复上下文继续工作 |
| /daily-summary | Command | Phase 5 | 生成单模块日报 |
| /sync-progress 🆕 | Command | Phase 5 | **PM 工具包** - 同步进度到甘特图 |
| /daily-standup 🆕 | Command | Phase 5 | **PM 工具包** - 生成项目级站会摘要 |
| progress_updater | Skill | Phase 5 | 自动更新 PROGRESS_LOG |

### P1 - 高优先级

| 工具 | 类型 | 阶段 |
|------|------|------|
| /check-progress | Command | Phase 5 |
| ui_demo | Skill | Phase 3 |
| review_alignment | Skill | Phase 5 |
| doc_generator | Skill | Phase All |
| spec_writer | Subagent | Phase 2 |
| progress_tracker | Subagent | Phase 5 |

### P2 - 标准优先级

| 工具 | 类型 | 阶段 |
|------|------|------|
| /gen-demo | Command | Phase 3 |
| /run-tests | Command | Phase 6 |
| /release | Command | Phase 7 |
| spec_validator | Skill | Phase 2 |
| mock_api_generator | Skill | Phase 3 |
| test_runner | Skill | Phase 6 |
| changelog_updater | Skill | Phase All |
| test_plan_writer | Subagent | Phase 6 |
| release_summarizer | Subagent | Phase 7 |

### P3 - 低优先级

| 工具 | 类型 | 阶段 |
|------|------|------|
| /init-project | Command | Phase 0 |

---

## 6. PM 工具包说明

项目经理（PM）日常工作中最重要的两个命令：

### 🔄 /sync-progress
- **作用**：将各功能模块的进度数据同步到甘特图看板
- **使用场景**：
  - 开发完成任务后，更新看板视图
  - 每日站会前，同步最新进度
- **数据流**：`PROGRESS_LOG.yaml` → `projectRegistry.ts` → 甘特图 UI

### 📋 /daily-standup
- **作用**：自动汇总所有功能模块的进度，生成项目级站会报告
- **使用场景**：
  - 每日站会前自动生成摘要
  - 快速了解项目整体状态
- **数据流**：各模块 `PROGRESS_LOG.yaml` → `PROJECT_DAILY_STANDUP.yaml` → Daily Standup 面板

---

## 7. 审核检查清单

审核工具定义时，请确认：

- [ ] 用途描述清晰
- [ ] 输入/输出定义明确
- [ ] 执行逻辑可行
- [ ] 优先级合理
- [ ] 与其他工具不冲突
- [ ] 符合项目实际需求

---

## 附录：工具统计

| 类型 | 总数 | P0 | P1 | P2 | P3 |
|------|------|-----|-----|-----|-----|
| Slash Commands | 10 | 5 | 1 | 3 | 1 |
| Skills | 8 | 1 | 3 | 4 | 0 |
| Subagents | 4 | 0 | 2 | 2 | 0 |
| **总计** | **22** | 6 | 6 | 9 | 1 |

### PM 工具包
| 工具 | 用途 | 优先级 |
|------|------|--------|
| /sync-progress | 同步进度到甘特图 | P0 |
| /daily-standup | 生成站会摘要 | P0 |

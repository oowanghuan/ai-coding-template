# Expert Review Report

> **功能模块**: {{feature}}
> **评审阶段**: Phase {{phase}}
> **评审文件**: {{target_file}}
> **评审时间**: {{reviewed_at}}
> **评审模型**: OpenAI {{model}}
> **版本**: v1.0

---

## 1. 评审结论

### {{verdict}}

{{verdict_description}}

| 判定 | 含义 |
|------|------|
| **GO** | 无阻断问题，可以继续 |
| **REVISE** | 有改进建议，建议修复后继续 |
| **BLOCK** | 有严重问题，必须修复后重新评审 |

---

## 2. 问题统计

| 级别 | 数量 | 说明 |
|------|------|------|
| Block | {{block_count}} | 必须修复，否则无法进入下一阶段 |
| Warn | {{warn_count}} | 建议修复，但不阻断流程 |
| **总计** | **{{total_issues}}** | |

---

## 3. Block 级问题

{{#if block_issues}}
{{#each block_issues}}
### {{id}}: {{issue}}

| 属性 | 值 |
|------|------|
| **严重性** | Block |
| **类别** | {{category}} |
| **位置** | {{target}} - {{location}} |
| **影响** | {{impact}} |

**问题描述**:
{{issue}}

**证据**:
> {{evidence}}

**修复建议**:
{{fix}}

**负责角色**: {{owner_role}}

---
{{/each}}
{{else}}
无 Block 级问题。
{{/if}}

---

## 4. Warn 级问题

{{#if warn_issues}}
{{#each warn_issues}}
### {{id}}: {{issue}}

| 属性 | 值 |
|------|------|
| **严重性** | Warn |
| **类别** | {{category}} |
| **位置** | {{target}} - {{location}} |

**问题描述**:
{{issue}}

**修复建议**:
{{fix}}

**负责角色**: {{owner_role}}

---
{{/each}}
{{else}}
无 Warn 级问题。
{{/if}}

---

## 5. 下一步操作

{{#if is_block}}
### 必须执行

1. 修复所有 **Block 级问题**（共 {{block_count}} 个）
2. 重新执行评审：`/expert-review {{feature_path}}`
3. 确认 verdict 变为 GO 或 REVISE 后继续

**注意**: 当前 External Gate 已阻断，`/next-phase` 命令将被拒绝。
{{else if is_revise}}
### 建议执行

1. 检查 **Warn 级问题**（共 {{warn_count}} 个）
2. 根据优先级决定是否修复
3. 可直接执行 `/next-phase {{feature_name}}` 继续

**提示**: Warn 级问题不阻断流程，但建议在后续迭代中修复。
{{else}}
### 可以继续

评审通过，可执行 `/next-phase {{feature_name}}` 进入下一阶段。
{{/if}}

---

## 6. 评审依据

本次评审基于以下硬规则（违反必须 BLOCK）：

1. **Gate 可被绕过** - 存在绕过 Phase Gate 的路径
2. **状态/配置混写** - 运行态状态与静态配置混合存储
3. **审批形同虚设** - 审批机制无实际约束力
4. **设计无法被测试验证** - 缺乏可测试性
5. **存在安全/失控风险** - 如 eval、越权写入等

---

## 7. 相关文件

| 文件 | 说明 |
|------|------|
| `REVIEW_ACTIONS.yaml` | 结构化问题清单（机器可读） |
| `{{target_file}}` | 被评审文件 |
| `90_PROGRESS_LOG.yaml` | 进度日志 |

---

_Generated by `/expert-review` | {{reviewed_at}}_

# AI 协作开发框架 - 每日工作详解

> 适用于：开发过程中查阅 | 包含详细的人机协作示例

---

## 一、每日工作循环

### 1.1 标准流程

```
┌─────────────────────────────────────────────────────────────────────┐
│                         每日工作循环                                  │
└─────────────────────────────────────────────────────────────────────┘

上班
  │
  ▼
┌──────────────┐
│ /start-day   │ ← 同步代码，显示待办
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ /iresume     │ ← 恢复上下文（最重要！）
└──────┬───────┘
       │
       ▼
┌──────────────┐
│   开发工作    │ ← 编码、测试、文档
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ /end-day     │ ← 保存进度，提交代码
└──────────────┘
  │
下班
```

### 1.2 命令详解

**`/start-day`** - 每日开始

```bash
/start-day
```

执行内容：
1. 执行 `git pull` 同步远程代码
2. 扫描活跃的功能模块
3. 显示当前进度和待办任务
4. 恢复工作上下文

输出示例：
```
🌅 每日开始 - user-auth

📥 Git 同步: ✅ 已同步最新代码
📍 上下文恢复:
  • 功能模块: user-auth
  • 当前阶段: Phase 5 - Code
  • 整体进度: 45%
  • 上次操作: 完成登录表单 UI

📋 今日待办:
  ⏭️ 下一步: 实现登录 API 调用
  🔄 进行中: [CODE-004] 实现登录 API 调用
```

**`/iresume {feature}`** - 断点恢复

```bash
/iresume user-auth
```

**这是最重要的命令！** 当对话变长、Claude 开始遗忘时，立即执行。

执行内容：
1. 读取 `90_PROGRESS_LOG.yaml`
2. 读取 `cc_checkpoint` 断点信息
3. 加载 `context_files` 相关文件
4. 恢复完整工作上下文

**`/end-day`** - 每日结束

```bash
/end-day
```

执行内容：
1. 询问今日完成的任务
2. 更新 `90_PROGRESS_LOG.yaml`
3. 更新 `cc_checkpoint` 断点信息
4. 生成每日总结
5. 执行 `git add` + `git commit`
6. 确认后执行 `git push`

---

## 二、Phase 5 人机协作详解

### 2.1 核心文件：90_PROGRESS_LOG.yaml

```yaml
meta:
  feature: "user-auth"
  current_phase: 5
  status: wip
  last_updated: "2024-12-16T10:30:00"

phase_5_code:
  status: wip
  tasks:
    - id: CODE-001
      task: "创建 auth 模块目录结构"
      status: done
      completed_at: "2024-12-15"

    - id: CODE-002
      task: "实现登录页面 UI"
      status: done
      completed_at: "2024-12-15"

    - id: CODE-003
      task: "实现登录 API 调用"
      status: wip                     # 当前进行中
      notes: "正在处理错误响应"

    - id: CODE-004
      task: "实现 Token 存储"
      status: pending

# 断点恢复信息（关键！）
cc_checkpoint:
  session_id: "cc-2024-12-16-user-auth"
  last_file_edited: "src/auth/api.ts"
  last_action: "完成登录 API 基础调用，待处理错误响应"
  next_step: "添加错误处理和重试逻辑"
  context_files:
    - "docs/user-auth/40_DESIGN_FINAL.md"
    - "docs/user-auth/90_PROGRESS_LOG.yaml"
    - "src/auth/api.ts"
    - "src/auth/login.vue"
```

**cc_checkpoint 字段说明**：
- `session_id`：标识工作会话
- `last_file_edited`：上次编辑的文件（用于快速定位）
- `last_action`：上次完成的操作（人类可读）
- `next_step`：下一步要做什么（恢复时的起点）
- `context_files`：相关文件列表（恢复时自动加载）

### 2.2 典型工作日对话示例：Day 1

```
┌─────────────────────────────────────────────────────────────┐
│ 👤 Human                        │ 🤖 Claude Code CLI        │
├─────────────────────────────────┼───────────────────────────┤
│                                 │                           │
│ /start-day user-auth            │                           │
│                                 │ → git pull origin main    │
│                                 │ → 读取 90_PROGRESS_LOG    │
│                                 │ → 显示当前状态            │
│                                 │                           │
│                                 │ 📍 Phase 5 Code           │
│                                 │ 进度: 25%                 │
│                                 │ 下一步: CODE-003 登录API  │
│                                 │                           │
├─────────────────────────────────┼───────────────────────────┤
│                                 │                           │
│ "开始实现登录 API"              │                           │
│                                 │ → 读取 40_DESIGN_FINAL.md │
│                                 │ → 读取现有代码            │
│                                 │ → 开始编写 api.ts         │
│                                 │                           │
│                                 │ ✅ 已创建 src/auth/api.ts │
│                                 │ 实现了 login() 函数       │
│                                 │                           │
├─────────────────────────────────┼───────────────────────────┤
│                                 │                           │
│ "好的，先到这里，明天继续"      │                           │
│                                 │                           │
│ /end-day                        │                           │
│                                 │ → 询问今日完成情况        │
│                                 │                           │
│ "CODE-003 进行中，完成了基础    │                           │
│  调用，错误处理明天做"          │                           │
│                                 │                           │
│                                 │ → 更新 PROGRESS_LOG:      │
│                                 │   CODE-003.status = wip   │
│                                 │   CODE-003.notes = "..."  │
│                                 │                           │
│                                 │ → 更新 cc_checkpoint:     │
│                                 │   last_file = api.ts      │
│                                 │   last_action = "完成.."  │
│                                 │   next_step = "添加错误.."│
│                                 │                           │
│                                 │ → git add . && git commit │
│                                 │ → git push (确认后)       │
│                                 │                           │
│                                 │ 🌙 下班流程完成！         │
│                                 │                           │
└─────────────────────────────────┴───────────────────────────┘
```

### 2.3 断点恢复：Day 2

```
┌─────────────────────────────────────────────────────────────┐
│ 👤 Human                        │ 🤖 Claude Code CLI        │
├─────────────────────────────────┼───────────────────────────┤
│                                 │                           │
│ /start-day                      │                           │
│                                 │ → git pull origin main    │
│                                 │ → 找到 user-auth (wip)    │
│                                 │ → 读取 cc_checkpoint      │
│                                 │                           │
│                                 │ ┌─────────────────────┐   │
│                                 │ │ 📍 断点恢复         │   │
│                                 │ │                     │   │
│                                 │ │ 功能: user-auth     │   │
│                                 │ │ 阶段: Phase 5 Code  │   │
│                                 │ │ 进度: 50%           │   │
│                                 │ │                     │   │
│                                 │ │ 🕐 上次状态:        │   │
│                                 │ │ 文件: src/auth/api.ts│  │
│                                 │ │ 操作: 完成登录基础  │   │
│                                 │ │       调用          │   │
│                                 │ │                     │   │
│                                 │ │ ⏭️ 下一步:          │   │
│                                 │ │ 添加错误处理和      │   │
│                                 │ │ 重试逻辑            │   │
│                                 │ └─────────────────────┘   │
│                                 │                           │
│                                 │ → 自动加载 context_files  │
│                                 │ → 恢复工作上下文          │
│                                 │                           │
├─────────────────────────────────┼───────────────────────────┤
│                                 │                           │
│ "继续昨天的工作，添加错误处理"  │                           │
│                                 │ → CC 已知道上下文         │
│                                 │ → 直接编辑 api.ts         │
│                                 │ → 添加 try-catch          │
│                                 │ → 添加重试逻辑            │
│                                 │                           │
│                                 │ ✅ 已更新 src/auth/api.ts │
│                                 │ 添加了错误处理和重试      │
│                                 │                           │
└─────────────────────────────────┴───────────────────────────┘
```

### 2.4 对话过长时的恢复

当对话变得很长，Claude 可能会丢失上下文：

```
┌─────────────────────────────────────────────────────────────┐
│ 👤 Human                        │ 🤖 Claude Code CLI        │
├─────────────────────────────────┼───────────────────────────┤
│                                 │                           │
│ (对话已经很长了...)             │                           │
│                                 │                           │
│ "继续实现 Token 存储"           │                           │
│                                 │ ❓ 我需要更多上下文       │
│                                 │    来理解当前任务...      │
│                                 │                           │
├─────────────────────────────────┼───────────────────────────┤
│                                 │                           │
│ /iresume user-auth              │  ← 立即执行！              │
│                                 │ → 读取 90_PROGRESS_LOG    │
│                                 │ → 读取 cc_checkpoint      │
│                                 │ → 读取 context_files      │
│                                 │                           │
│                                 │ ┌─────────────────────┐   │
│                                 │ │ 📍 上下文已恢复     │   │
│                                 │ │                     │   │
│                                 │ │ 当前任务:           │   │
│                                 │ │ CODE-004 Token存储  │   │
│                                 │ │                     │   │
│                                 │ │ 已读取文件:         │   │
│                                 │ │ ✅ 40_DESIGN_FINAL  │   │
│                                 │ │ ✅ src/auth/api.ts  │   │
│                                 │ │ ✅ src/auth/store.ts│   │
│                                 │ └─────────────────────┘   │
│                                 │                           │
├─────────────────────────────────┼───────────────────────────┤
│                                 │                           │
│ "好，继续实现"                  │                           │
│                                 │ → 现在有完整上下文了      │
│                                 │ → 继续编写代码...         │
│                                 │                           │
└─────────────────────────────────┴───────────────────────────┘
```

### 2.5 进度更新时机

**CC 自动更新进度的时机**：

| 时机 | 更新内容 |
|------|----------|
| 完成一个任务 | `task.status = done`, `task.completed_at` |
| 开始新任务 | `task.status = wip` |
| 遇到阻塞 | `task.status = blocked`, `task.notes` |
| `/end-day` | `cc_checkpoint` 全部字段 |
| 文件编辑后 | `cc_checkpoint.last_file_edited` |

**Human 需要参与的时机**：

| 时机 | Human 动作 |
|------|------------|
| 任务完成确认 | 告诉 CC "这个任务完成了" |
| 下班前 | 执行 `/end-day`，确认今日完成情况 |
| 遇到问题 | 告诉 CC 当前状态和阻塞原因 |
| 切换任务 | 告诉 CC "先做另一个任务" |

---

## 三、Phase 6 测试工具详解

### 3.1 Chrome DevTools MCP 简介

Chrome DevTools MCP 让 Claude Code 可以直接控制浏览器进行 UI 测试。

**核心能力**：

| 能力 | MCP 工具 | 说明 |
|------|----------|------|
| 页面导航 | `navigate_page`, `new_page` | 打开/跳转页面 |
| 页面快照 | `take_snapshot`, `take_screenshot` | 获取页面状态 |
| 元素交互 | `click`, `fill`, `hover` | 点击、输入、悬停 |
| 表单操作 | `fill_form` | 批量填写表单 |
| 等待机制 | `wait_for` | 等待元素/文本出现 |
| 控制台 | `list_console_messages` | 查看 console 日志 |
| 网络请求 | `list_network_requests` | 查看 API 请求 |

### 3.2 UI 测试示例

```
┌─────────────────────────────────────────────────────────────┐
│ 👤 Human                        │ 🤖 Claude Code CLI        │
├─────────────────────────────────┼───────────────────────────┤
│                                 │                           │
│ "测试登录页面的 UI 交互"        │                           │
│                                 │ → 打开浏览器页面          │
│                                 │   mcp: new_page           │
│                                 │   url: localhost:3000     │
│                                 │                           │
│                                 │ → 获取页面快照            │
│                                 │   mcp: take_snapshot      │
│                                 │                           │
│                                 │ 📸 页面结构:              │
│                                 │ - input[uid="u1"] 用户名  │
│                                 │ - input[uid="u2"] 密码    │
│                                 │ - button[uid="u3"] 登录   │
│                                 │                           │
├─────────────────────────────────┼───────────────────────────┤
│                                 │                           │
│ "测试正常登录流程"              │                           │
│                                 │ → 填写表单                │
│                                 │   mcp: fill_form          │
│                                 │   elements:               │
│                                 │   - uid: u1               │
│                                 │     value: testuser       │
│                                 │   - uid: u2               │
│                                 │     value: password123    │
│                                 │                           │
│                                 │ → 点击登录按钮            │
│                                 │   mcp: click uid: u3      │
│                                 │                           │
│                                 │ → 等待跳转                │
│                                 │   mcp: wait_for           │
│                                 │   text: "欢迎回来"        │
│                                 │                           │
│                                 │ ✅ 测试通过：             │
│                                 │ 登录成功，跳转到首页      │
│                                 │                           │
└─────────────────────────────────┴───────────────────────────┘
```

### 3.3 常用 MCP 命令

**页面导航**：
```yaml
# 打开新页面
mcp: new_page
url: "http://localhost:3000/login"

# 页面跳转
mcp: navigate_page
type: url
url: "/dashboard"

# 返回/前进/刷新
mcp: navigate_page
type: back  # 或 forward, reload
```

**元素交互**：
```yaml
# 点击元素
mcp: click
uid: "u123"

# 输入文本
mcp: fill
uid: "u123"
value: "输入的内容"

# 批量填写表单（推荐）
mcp: fill_form
elements:
  - uid: "u1"
    value: "username"
  - uid: "u2"
    value: "password"
```

**等待和验证**：
```yaml
# 等待文本出现
mcp: wait_for
text: "登录成功"
timeout: 5000

# 查看控制台日志
mcp: list_console_messages

# 查看网络请求
mcp: list_network_requests
resourceTypes: ["fetch", "xhr"]
```

### 3.4 测试失败处理策略

**问题分级标准**：

| 级别 | 定义 | 处理方式 | 示例 |
|------|------|----------|------|
| P0 | 功能完全不可用 | **立即修复** | 页面白屏、登录崩溃 |
| P1 | 核心流程受阻 | **立即修复** | 无法提交表单、跳转错误 |
| P2 | 功能异常但可绕过 | 记录，后续修复 | 错误提示不准确、样式错位 |
| P3 | 体验问题 | 记录，后续修复 | 按钮位置不理想、文案不清晰 |

**处理流程**：

```
测试用例执行
     │
     ▼
  通过？ ──Yes──▶ 记录 ✅
     │
    No
     │
     ▼
  判断级别
     │
  ┌──┴──┐
  │     │
P0/P1  P2/P3
  │     │
  ▼     ▼
暂停   记录 ❌
立即   继续下一个
修复
重跑
```

### 3.5 后端 API 测试

使用脚本进行 API 测试：

```bash
#!/bin/bash
# tests/api/auth.test.sh

BASE_URL="http://localhost:3000/api"

# 测试 1: 正常登录
echo "[TEST 1] 正常登录"
RESPONSE=$(curl -s -X POST "$BASE_URL/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username": "testuser", "password": "password123"}')

if echo "$RESPONSE" | grep -q '"token"'; then
  echo "✅ PASS"
else
  echo "❌ FAIL: $RESPONSE"
fi

# 测试 2: 错误密码
echo "[TEST 2] 错误密码"
RESPONSE=$(curl -s -X POST "$BASE_URL/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username": "testuser", "password": "wrong"}')

if echo "$RESPONSE" | grep -q '"error"'; then
  echo "✅ PASS"
else
  echo "❌ FAIL"
fi
```

---

## 四、常见场景处理

### 4.1 对话超长时恢复上下文

```bash
# 方法 1：使用 /iresume 恢复
/iresume user-auth

# 方法 2：查看进度获取上下文
/check-progress user-auth
```

### 4.2 多日开发同一功能

```bash
# Day 1 结束
/end-day user-auth  # 保存进度，提交代码

# Day 2 开始
/start-day user-auth  # 同步代码，恢复上下文

# Day 2 继续工作...

# Day 2 结束
/end-day user-auth
```

### 4.3 Gate 被阻断

```bash
# 检查阻断原因
/check-gate user-auth --phase=2

# 根据提示修复问题

# 重新检查
/check-gate user-auth --phase=2

# 审批通过
/approve-gate user-auth --phase=2 --role=PM
```

### 4.4 切换不同功能

```bash
# 功能 A
/iresume feature-a
# ... 工作 ...

# 切换到功能 B
/iresume feature-b
# ... 工作 ...

# 查看所有功能进度
/check-progress
```

### 4.5 跳过 Demo 阶段

如果功能没有 UI，编辑 `PHASE_GATE.yaml`：

```yaml
feature_profile:
  has_ui: false
  demo_required: false
```

Phase 3 将自动跳过。

---

## 五、最佳实践

### 5.1 上下文管理

1. **对话变长时主动恢复**：当感觉 Claude 开始遗忘，立即执行 `/iresume`
2. **下班前必做 /end-day**：保存所有断点信息
3. **早上第一件事 /start-day**：同步最新状态

### 5.2 进度追踪

1. **任务完成后立即告知**：让 Claude 更新 PROGRESS_LOG
2. **每日查看进度**：使用 `/check-progress`
3. **保持 cc_checkpoint 最新**：关键的恢复依据

### 5.3 测试执行

1. **先完成全部测试**：再修复问题
2. **P0/P1 立即修**：P2/P3 记录后修
3. **修复后回归测试**：确保没有引入新问题

---

## 下一步

- 想查阅完整命令参考？→ [04_REFERENCE.md](./04_REFERENCE.md)
- 想了解框架体系？→ [02_FRAMEWORK_OVERVIEW.md](./02_FRAMEWORK_OVERVIEW.md)
- 想快速上手？→ [01_QUICKSTART.md](./01_QUICKSTART.md)

---

_文档版本：v1.4 | 最后更新：2026-01-09_
